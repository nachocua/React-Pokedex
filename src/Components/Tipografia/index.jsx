import React from "react";
import "./index.css";

export const Title = (props) => {
   return <span className="tipografía title 32m">{props.children}</span>;
};

export const Body14 = (props) => {
   return <span className="tipografía title body14">{props.children}</span>;
};
